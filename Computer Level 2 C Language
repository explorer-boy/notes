	二叉树的前、中、后序遍历顺序
	结构化程序的基本结构有：顺序结构、选择（分支）结构和重复（循环）结构
	在关系数据库设计中，关系模式采用二维表来表示关系，简称表
	取模运算符%的左右两个操作数均应为整数
	z＝x＋＋，y＋＋，＋＋y；因为赋值运算符的优先级高于逗号运算符的优先级，所以可以将上式表示成(z＝x＋＋)，(y＋＋)，(＋＋y)
	条件表达式：x＝表达式1？表达式2：表达式3 的含义是：先求解表达式1，若为非0(真)，若表达式1的值为0(假)
	switch(expr1)，中的expr1不能用浮点类型或long类型，也不能为一个字符串。case后面常量表达式的类型必须与switch后面表达式的类型一致
	定义数组时必须为其指明长度
